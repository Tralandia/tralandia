entity: Entity\Location\Location

parameters:
	title: Locations
	h1: 'Location -> %name%'

grid:
	columns:
		id:
			label: Id
		name:
			label: Name
		nameOfficial:
			label: Name Official
		iso:
			label: Iso

form:
	fields:

		parent:
			label: Parent
			control:
				type: select
				repository: @locationRepositoryAccessor
				items: [getItems, id, name]
		map:
			label: GMap
			control: [type: map]